import React from 'react';
import ReactDOM from 'react-dom/client';
import ContextHook from './chpater_13/ContextHook.jsx';

const root = ReactDOM.createRoot(document.getElementById("root"));
  root.render(
    <div>
      <ContextHook />
    </div>
);
