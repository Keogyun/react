import React from 'react';
import ReactDOM from 'react-dom/client';
import PageColor from './chpater_13/PageColor.jsx';

const root = ReactDOM.createRoot(document.getElementById("root"));
  root.render(
    <div>
      <PageColor />
    </div>
);
