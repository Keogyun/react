import React from 'react';
import ReactDOM from 'react-dom/client';
import ContextApp from './chpater_13/ContextApp.jsx';

const root = ReactDOM.createRoot(document.getElementById("root"));
  root.render(
    <div>
      <ContextApp />
    </div>
);
